const validSchemaAuth = require('./validSchemaAuth')
const validSchemaAuthGoogle = require('./validSchemaAuthGoogle')

const users = {
  validSchemaAuth,
  validSchemaAuthGoogle,
}

module.exports = users
